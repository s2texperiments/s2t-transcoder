const base = "/tmp/data";
const inPath = `${dataPath}/in`;
const outPath = `${dataPath}/out`;

module.exports = {
    base: dataPath,
    in: `${dataPath}/in`,
    out:`${dataPath}/out`,
    report: `${dataPath}/report.json`
};